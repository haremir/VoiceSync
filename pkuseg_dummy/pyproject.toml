[project]
name = "pkuseg"
version = "0.0.25"
description = "Wrapper redirecting pkuseg to spacy-pkuseg"
requires-python = ">=3.11"
dependencies = [
    "spacy-pkuseg",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
